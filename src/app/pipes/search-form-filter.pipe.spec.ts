import { SearchFormFilterPipe } from './search-form-filter.pipe';

describe('SearchFormFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFormFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
